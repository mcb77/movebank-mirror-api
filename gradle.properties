# Use the local ../movebank-api-client and ../movebank-mirror checkouts during dev,
# instead of the published artifacts. Comment these out for a release/CI build.
#localMovebankApiClient=true
#localMovebankMirror=true
